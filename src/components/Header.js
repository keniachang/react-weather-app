const Header = () => {
    return (
        <header className='app-header'>
            <h1>Local Weather</h1>
            <input className='zip-code' type='text' placeholder='Zip Code in USA' />
        </header>
    )
};

export default Header;
