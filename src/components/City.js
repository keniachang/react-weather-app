const City = () => {
    return (
        <section className='sub-header'>
            <h1>City</h1>
            <span>Weather (Rain)</span>
            <h1>Temperature</h1>
            <span>min</span> <span>max</span>
        </section>
    );
};

export default City;
